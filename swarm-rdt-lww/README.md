# Swarm Last-Write-Wins RDT

This package only implements an `lww` RDT
that encodes objects and arrays with a trivial last-write-wins
merge behavior.

see https://github.com/gritzko/swarm-ron-docs/blob/2.0/rdt.md
